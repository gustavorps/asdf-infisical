# Contributing

Testing Locally:

```shell
asdf plugin test infisical https://github.com/gustavorps/asdf-infisical.git 'infisical --version'
```

Tests are automatically run in GitHub Actions on push and PR.
